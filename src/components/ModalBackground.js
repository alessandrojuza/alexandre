import React from "react";
import "../styles/ModalBackground.scss";

const ModalBackground = ({ modalClassName }) => {
  return <div className={`modal-backround ${modalClassName}`}></div>;
};

export default ModalBackground;
